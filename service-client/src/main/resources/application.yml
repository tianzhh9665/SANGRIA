spring:
  profiles:
    active: dev
  application:
    name: service-client
    

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/sangria/client/mapper/*Mapper.xml
  type-aliases-package: com.sangria.client.entity